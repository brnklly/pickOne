import React from "react";

const NotFound = () => {
  return (
    <main id="error">
      <div className="page-heading">These are not the droids...</div>
      <div className="sub-heading">
        Sorry but this page doesn't exist. Try something else.
      </div>
    </main>
  );
};

export default NotFound;
